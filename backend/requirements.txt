# --- web framework -----------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
python-dotenv==1.0.1
websockets==13.1

# --- agent orchestration -------------------------------------------------
langchain==0.3.14
langgraph==0.2.62

# LLM providers (you only need the package for the provider you set in
# LLM_PROVIDER, but installing all of them means you can switch any time)
langchain-anthropic==0.3.4
langchain-openai==0.3.1
langchain-groq==0.2.3
langchain-google-genai==2.0.8

# --- tools -----------------------------------------------------------------
ddgs==9.0.2            # free, keyless web search (used if TAVILY_API_KEY is unset)
requests==2.32.3        # used for the optional Tavily search backend
arxiv==2.1.3             # arXiv paper search
scikit-learn==1.6.1     # TF-IDF retrieval for the document-RAG tool
numpy==2.2.1
pypdf==5.1.0             # text extraction from uploaded PDFs
fpdf2==2.8.2             # PDF report generation
